# README - Rama: Tarea-1

Este README es específico para la rama `Tarea-1` del repositorio `desarrollo_web_Alonso_Araya`.

## Propósito
En esta rama se desarrolla la Tarea 1 del curso/actividad. Aquí se documentan los cambios, archivos y pruebas relacionados únicamente con esa tarea, sin afectar el README u otros archivos de la rama principal del repositorio.

## Contenido principal
- `index.html` (o el archivo principal de la tarea)
- `css/` (estilos usados en la tarea)
- `js/` (scripts usados en la tarea)
- Otros archivos o carpetas creadas para la tarea

(Ajusta esta lista según los archivos reales en la rama.)

## Cómo usar / probar
1. Cambiar a la rama `Tarea-1`:

   ```bash
   git fetch origin
   git checkout Tarea-1
   ```

2. Abrir `index.html` en el navegador o levantar un servidor local (opcional):

   ```bash
   # usando Python 3
   python -m http.server 8000
   # luego visitar http://localhost:8000
   ```

3. Revisar los archivos modificados y los commits relacionados con la tarea.

## Notas
- Este README está pensado para documentar exclusivamente lo relevante a la rama `Tarea-1`.
- Si quieres que haya documentación combinada en el README principal del repositorio, mantén las instrucciones generales en la rama default y esta versión enfocada en la tarea dentro de `Tarea-1`.

## Contacto
Para dudas o sugerencias, abre un issue o contacta al autor del repositorio.
